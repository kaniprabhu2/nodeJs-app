apiVersion: v1
kind: Service
metadata:
  name: nodejs-staging-service
spec:
  type: NodePort
  selector:
    app: nodejs
    env: staging
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30002
